using System;

namespace ChatClient
{
    public enum MessageTypes
    {
        UpdateMessage = 0,
        ExitMessage = 1,
        InitialMessage = 2,
        UserDisconnectedNotification = 3
    }
}
